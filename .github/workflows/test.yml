name: Tests

on:
  pull_request:
  push:
    # main is the only maintained branch. Keep this trigger aligned with the
    # default branch so retired aliases cannot hide CI drift.
    branches: [main]
  schedule:
    # Stagger sibling repositories so scheduled full matrices do not compete
    # for the same runner capacity at the top of the hour.
    - cron: "47 9 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  shell:
    # The full shared matrix supplies current Python on Linux, macOS, and
    # Termux. WSL's Windows process boundary is covered by deterministic command
    # fixtures because GitHub does not provide a native WSL hosted runner.
    uses: cgraf78/actions/.github/workflows/shell-ci.yml@464468328f050e43299298fc0eac8b971ddd38b7
    with:
      matrix-set: full
      profiles: base,python
      shellcheck-inventory-path: .github/shellcheck-files.txt
      test-command: VSCODE_EXTS_SKIP_SHELLCHECK=1 test/run

  actions-sync:
    name: cgraf78/actions sync
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      # GitHub resolves literal `uses:` refs before a job can read repository
      # files, so the lock cannot be interpolated. This gate verifies every
      # unavoidable reusable-workflow ref matches the reviewed dependency pin.
      - uses: cgraf78/actions/.github/actions/verify-consumer-sync@464468328f050e43299298fc0eac8b971ddd38b7
